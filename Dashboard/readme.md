# Dashboard

The interactive Shiny dashboard now lives at the repo root:

- `../app.py` — entry point (run with `shiny run app.py`).
- `../src/` — one module per tab plus shared data/geo/metrics/theme helpers.

This folder is kept for any future static assets (logos, exported screenshots).
